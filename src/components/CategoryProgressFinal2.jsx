 export default function CategoryProgressFinal2({value, maxValue, isPositive, className = "",}){const percentage = Math.min(100,(Math.abs(value)/ Math.abs(maxValue))* 100);return(<div className = {`w-full bg-gray-200 rounded-full h-2 ${className}`}> <div className = {`h-2 rounded-full transition-all ${isPositive ? "bg-green-500": "bg-red-500"}`}style = {{width: `${percentage}%`}}></div> </div>);}